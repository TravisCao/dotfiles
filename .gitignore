# Sensitive files
.credentials.json
**/secrets/
**/.env

# Claude session data (don't sync)
claude/projects/
claude/todos/
claude/shell-snapshots/
claude/ide/
claude/settings.json

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Backup directories
.dotfiles_backup_*

# Local machine-specific configurations
*.local
.zshrc.local
.bashrc.local

# Zsh generated files (machine-specific, should not be synced)
shell/.zcompdump*
shell/.zsh_history
shell/.zsh_sessions/
